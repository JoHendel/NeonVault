export class AudioSystem {
  play(_key: "hit" | "level-up" | "dash" | "boss-spawn" | "game-over"): void {
    // Audio hooks are intentionally quiet until real files are dropped in.
    // This keeps the project startable without placeholder binaries.
  }
}
