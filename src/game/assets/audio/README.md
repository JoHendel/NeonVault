# Audio Drop Zone

Add optional sound files here later if you want to wire real audio into the existing hooks:

- `hit.*`
- `level-up.*`
- `dash.*`
- `boss-spawn.*`
- `game-over.*`

The current build starts cleanly without audio assets.
